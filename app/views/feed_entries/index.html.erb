<div id="recent_episodes">
  <h3>All the blogs</h3>
  <ul>
  <% @blog_entries.each do |entry| %>

    <li><%= link_to entry.name, entry.url %></li>
    <li><%= simple_format(entry.summary) %></li>

  <% end %>
  </ul>
</div>
